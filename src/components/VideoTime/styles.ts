import styled from "styled-components";

export const VideoTime = styled.div`
  position:absolute;
  bottom: 12px;
  right:7px;
  border-radius: 2px;
  padding: 3px 4px;
  background: rgba(0, 0, 0, 0.5);
  p {
    font-size: 1rem;
    display: inline;
    color: #ffffff;
  }
`;
